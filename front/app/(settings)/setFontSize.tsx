import { BackHeader } from '@components';
import FontSizeScreenComponent from '../../components/Settings/fontSize';

export default function FontSizeScreen() {
    return (
        <>
            <BackHeader />
            <FontSizeScreenComponent />
        </>
    );
}
